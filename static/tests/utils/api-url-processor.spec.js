import { expect } from 'chai'

import { prepareUrl } from '../../src/utils/api-url-processor'

describe.skip('utils', () => {
  describe('api-url-processor', () => {
    it('should ...', () => {
      expect(prepareUrl()).to.be.defined
    })
  })
})
